# RESULT: "-- by PurC"

<!DOCTYPE hvml>
<hvml target="void">

    <iterate on 0 onlyif $L.lt($0~, 10) with $DATA.arith('+', $0~, 1) nosetotail silently>
        <inherit silently>
            $STREAM.stdout.writelines(
                $STR.join($?, ") Hello, world! -- from COROUTINE-", $CRTN.notexistence))
        </inherit>
    </iterate>

    "-- by $SYS.const('HVML_INTRPR_NAME')"
</hvml>

