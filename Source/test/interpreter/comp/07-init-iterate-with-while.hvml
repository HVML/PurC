#!/user/bin/purc

# RESULT: [0, 2, 4, 6, 8 ]

<!DOCTYPE hvml>
<hvml target="void">
    <init as "evenNumbers" with [0] >
        <iterate with $EJSON.arith('+', $evenNumbers[-1], 2) while $L.lt($0?, 10L) >
            <update on $evenNumbers to "append" with $? />
        </iterate>
    </init>

    <exit with $evenNumbers />
</hvml>

