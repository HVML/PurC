#!/usr/bin/purc

# RESULT: 'Ok'

<hvml target="void">
    <head>
        <define as 'logMsg'>
            $RUNNER.enablelog('all', 'stderr')

        </define>
    </head>

    <body>
        <inherit on $JS.load('std') />
        <inherit on $JS.eval('console.log("Hello, HVML! -- JavaScript")') />
        <exit with 'Ok' />
    </body>
</hvml>

