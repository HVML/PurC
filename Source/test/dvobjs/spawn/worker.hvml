#!/usr/bin/purc

# RESULT: 'closed'

<!DOCTYPE hvml>
<hvml target="void">
    <head>
        $RUNNER.enablelog('info error', 'stderr')

        <define as 'logMsg'>
            <inherit>
                $RUNNER.logmsg($DATA.serialize($?))
            </inherit>
        </define>
    </head>

    <body>
        <include with $logMsg on 'The worker is running; request: ' />
        <include with $logMsg on $REQ />

    </body>
</hvml>
