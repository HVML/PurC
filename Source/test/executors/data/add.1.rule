# state: `begin` | `in_sample`
# sample type: `undefined` | `positive` | `negative`
# <begin>/comment_line:  ^'#' .* '\n'
# <begin>/sample_type:   ^('P'|'N') ':' .* '\n' { set sample_type; }
# <begin>/blank_line:    ^[ \t]* '\n'
# <begin>/@default:                             { switch to in_sample; }
# <sample>sample_record: ^(.* '\n')* ';' '\n'   { process sample;
#                                                 switch to begin; }


# blank lines
                      

P: # bellow are positive samples
# blank lines
                      

# all seperated with a SPACE
ADD : FROM 0 ;
ADD : FROM 0 TO 10 ;
ADD : FROM 0 TO 10 , ADVANCE 2 ;
ADD : FROM 0 TO 10 , ADVANCE 1 + 2 ;
ADD : FROM 0 TO 10 , ADVANCE 1 + 2 / 4 - 3 * 5 ;
ADD : FROM 0 TO 10 , ADVANCE 1 + 2 / ( 4 - 3 ) * 5 ;
# no SPACE in between
ADD:FROM 0 TO 10,ADVANCE 1+2/(4-3)*5;
# multiple line
ADD:
        FROM 0
        TO 1
        +
        2
        /
        (
        4
        -
        3
        )
        *
        5
        ,
        ADVANCE 1
        +
        2
        /
        (
        4
        -
        3
        )
        *
        5
        ;

N: # bellow are negative samples
# blank lines
                      

# SPACE required
ADD:FROM0 TO 10,ADVANCE 1;
ADD:FROM 0TO 10,ADVANCE 1;
ADD:FROM 0 TO10,ADVANCE 1;
ADD:FROM 0 TO 10,ADVANCE1;

# '\n' in wrong place
ADD
:FROM 0 TO 10,ADVANCE 1;

ADD: FROM
0 TO 10,ADVANCE 1;

ADD: FROM 0 TO
10,ADVANCE 1;

ADD: FROM 0 TO 10, ADVANCE
1;

ADD: FROM 0TO 10, ADVANCE
1;

