<hvml>
    <div>
        $DOC.query("#c_query").count()
    </div>
    <bhoose on="$DOC.query('#c_query').count()">
    </bhoose>
    <choose on="$DOC.query('#c_query').count()">
    </choose>
    <choose on="$DOC.query('#c_query').attr.value">
    </choose>
    <choose on="$MATH.eval($DOC.query('#expression').attr('value'))">
    </choose>
</hvml>
